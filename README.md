# Welcome to render-vape source code, in here you cannot leak anything you see!
## If you leak any of these files here you will be perm banned off render-vape server.

### File deleted temp due to storage being leaked fr :(

## Render-Vape Loader
```lua
loadfile('rendervape/core/boot.lua')()
```

## Render-Vape Premium Loader
```lua
getgenv().RenderIntents = {
    Premium = true
};
loadfile('rendervape/core/boot.lua');
```
